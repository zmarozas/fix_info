The genuinely shared stuff is already in the base (constants, build*
factories, initMocks). The @Mock fields differ per subclass — the
listener test mocks the handlers + scheduler, the translator test
mocks none — so there isn't a common set to hoist without giving
subclasses mocks they don't use. If you're seeing a specific mock
repeated across all of them, point me at it and I'll dedupe.