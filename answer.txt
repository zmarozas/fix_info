
The wedge is this, MagicsAdapter.setup():
 
    RetryPolicy.builder().build().tryCall(() -> session.connect());
 
Kerberos login can never succeed (missing JAAS config), tryCall retries forever,
and it runs inside handle(AepEngineActiveEvent) - on the engine's event thread.
Handler never returns -> engine consumes nothing -> shutdown message sits on the
queue. Stuck starting, which blocks stopping.
 
Fix: kill -9, fix SolaceGSS JAAS config on the host, restart. Follow-up: bound
the retry so the existing "exiting app" catch in StockloanGatewayMain actually
fires - today tryCall swallows every exception before it. Same pattern exists in
BifrostAdapter.setup(); I'll fix it there too.
 